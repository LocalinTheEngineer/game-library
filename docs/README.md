Screenshots referenced by the project README go here:

- `library.png` — the library grid with cover art
- `dashboard.png` — stats strip and current games
- `stats.png` — charts

Take them at a reasonably wide browser window so the grid shows several columns.
